# smart-shopper

Compare prices of grocery items from different grocery chains for the cheapest item

## Install

```sh
# Ruby
bundle install
# JS
npm install
```

Only a dev script currently exists, but it will start both Ruby and JS bundlers:

```sh
npm run dev
```
